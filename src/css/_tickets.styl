.tickets {
    display: grid;
    grid-template-columns: 30% 1fr;
    grid-gap: 20px;
    padding: 8%;
    &__filters {
        padding: 1rem;
        background: #fff;
        height: 40rem;
        box-shadow: 0 1px 5px 0 rgba(0,0,0,.15);
    }

}